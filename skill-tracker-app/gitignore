# Abhängigkeiten (werden per npm install neu erzeugt)
node_modules/

# Build-Ausgabe (wird per npm run build/publish neu erzeugt)
dist/

# Logs
*.log
npm-debug.log*

# Betriebssystem-Kram
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Umgebungsvariablen (Token NICHT committen!)
.env
